
# 📘 API Endpoints

## `GET /`

**Description**: Health check for the API.

**Response**:
```json
{
  "message": "APP IS RUNNING"
}
```

---

## `POST /upload`

**Description**: Uploads a file, converts it to markdown, splits it into chunks, and stores embeddings in ChromaDB.

**Request**:
- **Content-Type**: `multipart/form-data`
- **Form Fields**:
  - `file`: `UploadFile` — A markdown or compatible file.

**Response**:
```json
{
  "status": "uploaded"
}
```

**Error**:
- `500 Internal Server Error` — If the upload or processing fails.

---

## `POST /search`

**Description**: Performs semantic search and generates an LLM-based answer using documents retrieved from the vectorstore.

**Request**:
- **Content-Type**: `application/x-www-form-urlencoded`
- **Form Fields**:
  - `query`: `string` — A natural language query.

**Response**:
```json
{
  "response": "Answer generated by the model"
}
```

**Error**:
- `500 Internal Server Error` — If query processing fails.

---

## `GET /list_documents`

**Description**: Lists all documents currently stored in the ChromaDB collection.

**Response**:
```json
{
  "documents": [
    {
      "content": "Chunk of text",
      "metadata": {
        "filename": "example.md",
        "page": 1
      }
    },
    ...
  ]
}
```

---

## `POST /reset_chromadb`

**Description**: Clears all documents from the ChromaDB vectorstore.

**Response**:
```json
{
  "status": "ChromaDB reset successfully"
}
```

**Error**:
- `500 Internal Server Error` — If the reset fails.

---
